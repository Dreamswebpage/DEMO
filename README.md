# Your Site Name

This is a Next.js project for a certificate link sharing site.

## Setup
1. Install dependencies: `npm install`
2. Set up environment variables in `.env.local`.
3. Run locally: `npm run dev`
4. Deploy on Netlify.

## Features
- Home page with certificate links.
- Student login and dashboard.
- Admin panel.
- AdSense integration.